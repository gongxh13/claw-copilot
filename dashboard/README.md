# Dashboard Workspace

This directory is reserved for the standalone Claw Copilot dashboard app.

Planned stack:

- React
- Vite
- TypeScript
- PWA support

The OpenClaw plugin in `plugin/` will provide the API routes and serve the built static assets from this workspace once the dashboard is implemented.
