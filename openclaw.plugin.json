{
  "id": "claw-copilot",
  "name": "Claw Copilot",
  "description": "Run-aware observability dashboard for OpenClaw sessions",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "basePath": {
        "type": "string",
        "default": "/claw-copilot"
      },
      "dashboardTitle": {
        "type": "string",
        "default": "Claw Copilot"
      },
      "imVerbosity": {
        "type": "string",
        "enum": ["silent", "normal", "verbose"],
        "default": "normal"
      }
    }
  },
  "uiHints": {
    "basePath": {
      "label": "Base path",
      "help": "HTTP mount path for the dashboard routes"
    },
    "dashboardTitle": {
      "label": "Dashboard title"
    }
  }
}
